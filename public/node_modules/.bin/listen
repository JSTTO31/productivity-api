#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../ipx/node_modules/listhen/bin/listhen.mjs" "$@"
  ret=$?
else 
  node  "$basedir/../ipx/node_modules/listhen/bin/listhen.mjs" "$@"
  ret=$?
fi
exit $ret
